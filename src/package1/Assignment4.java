package package1;

public class Assignment4 {
    public static void main(String[] args) {
        String s = "Hello";
       // System.out.println(s);      // prints the string "Hello"
        for (int i = 4; -1 < i && i < s.length(); i--) { //index starts at 4 and runs in reverse order (i--) then stops before index reaches -1.
            System.out.print(s.charAt(i));    // prints the characters at the index "i" on the same line.
        }
        
        /*String s = "Hello";
        String s2 = "hello" + s;

        System.out.println(s2);*/

        

    
    
    
    }
    
}
